# Agent Behavioral Rules for CineSync

- **Git Push Policy**: Do NOT automatically push changes to GitHub (`git push`). Make changes, test locally, and present them for local testing. Only execute `git push` when the user explicitly requests a push to GitHub.
